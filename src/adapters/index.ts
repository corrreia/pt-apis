/**
 * Adapter barrel file.
 *
 * Import every adapter here so it registers itself in the global registry
 * at module load time. The order does not matter.
 *
 * When you create a new adapter, add a single import line below.
 */

import "./ipma/adapter";
import "./qualidade-ar/adapter";
